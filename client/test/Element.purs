module Test.Element where

import Prelude
import Test.Unit
import Test.Helpers

tests = 
  suite "element" do
    test "reconcile" do
      notWritten
    test "overlap" do
      notWritten
    test "inser tkey" do
      notWritten
    test "set time" do
      notWritten
    test "find moment" do
      notWritten