module Test.ElementEditor where

import Prelude

import Test.Unit
import Test.Helpers
tests = 
  suite "Element Editor" do
    test "Form Change" do
      notWritten
    test "Add Moment" do
      notWritten
    test "Handle Input" do
      notWritten