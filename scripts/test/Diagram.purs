module Test.Diagram where

import Prelude
import Test.Unit
import Test.Helpers

tests = 
  suite "Diagram" do
    test "Tick" do
      notWritten
    test "SetTime" do
      notWritten
    test "Change the Target" do
      notWritten
    test "Deal with a click" do
      notWritten